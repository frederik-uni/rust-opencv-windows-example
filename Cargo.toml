[package]
name = "opencv-example"
version = "0.1.0"
edition = "2024"

[dependencies]
opencv = { version = "0.94.4", default-features = false, features = [
    "imgproc",
    "clang-runtime",
] }
image = "0.25"
